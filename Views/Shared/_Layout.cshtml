<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - T-Soft ERP</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <aside class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <div style="display: flex; align-items: center; gap: 16px; flex: 1;">
                <div class="brand-icon">📦</div>
                <div class="brand-info">
                    <h2 class="brand-title">T-Soft ERP</h2>
                    <p class="brand-subtitle">E-Ticaret Yönetimi</p>
                </div>
            </div>
            <button class="sidebar-close" onclick="toggleSidebar()" title="Kapat">✕</button>
        </div>

        <nav class="sidebar-nav">
            <a href="/" class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Home" ? "active" : "")">
                <span class="nav-icon">📊</span>
                <span class="nav-text">Dashboard</span>
            </a>
            <a href="/Products" class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "ProductsMvc" || ViewContext.RouteData.Values["Controller"]?.ToString() == "Products" ? "active" : "")">
                <span class="nav-icon">📦</span>
                <span class="nav-text">Ürünler</span>
            </a>
            <a href="/Categories" class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "CategoriesMvc" || ViewContext.RouteData.Values["Controller"]?.ToString() == "Categories" ? "active" : "")">
                <span class="nav-icon">📁</span>
                <span class="nav-text">Kategoriler</span>
            </a>
            <a href="/Orders" class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "OrdersMvc" || ViewContext.RouteData.Values["Controller"]?.ToString() == "Orders" ? "active" : "")">
                <span class="nav-icon">🛒</span>
                <span class="nav-text">Siparişler</span>
            </a>
            <a href="/Warehouses" class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "WarehouseMvc" ? "active" : "")">
                <span class="nav-icon">🏭</span>
                <span class="nav-text">Depo Yönetimi</span>
            </a>

            @if (User?.IsInRole("Admin") == true)
            {
                <div style="margin: 16px 0; border-top: 1px solid rgba(124, 58, 237, 0.15);"></div>
                <a href="/Admin" class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Admin" || ViewContext.RouteData.Values["Controller"]?.ToString() == "AdminUsers" ? "active" : "")" style="background: rgba(239, 68, 68, 0.1); border-color: rgba(239, 68, 68, 0.3);">
                    <span class="nav-icon">👑</span>
                    <span class="nav-text">Admin Panel</span>
                </a>
            }

            <div style="margin: 16px 0; border-top: 1px solid rgba(124, 58, 237, 0.15);"></div>
            <a href="/Test" class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Test" ? "active" : "")">
                <span class="nav-icon">🧪</span>
                <span class="nav-text">API Test</span>
            </a>
            <a href="/api/docs" target="_blank" class="nav-item">
                <span class="nav-icon">📡</span>
                <span class="nav-text">API Docs</span>
            </a>
        </nav>

        <div class="sidebar-footer">
            <!-- User Info Box -->
            <div class="user-info-box">
                <div class="user-avatar">
                    <span>@(User?.Identity?.Name?.Substring(0, 1).ToUpper() ?? "?")</span>
                </div>
                <div class="user-details">
                    <div class="user-name">@(User?.Identity?.Name ?? "Misafir")</div>
                    <div class="user-role">
                        @if (User?.IsInRole("Admin") == true)
                        {
                            <span class="role-badge role-admin">👑 Admin</span>
                        }
                        else if (User?.IsInRole("Manager") == true)
                        {
                            <span class="role-badge role-manager">👨‍💼 Manager</span>
                        }
                        else if (User?.IsInRole("Viewer") == true)
                        {
                            <span class="role-badge role-viewer">👁️ Viewer</span>
                        }
                        else
                        {
                            <span class="role-badge role-guest">👤 Misafir</span>
                        }
                    </div>
                </div>
            </div>

            <!-- Logout Button -->
            @if (User?.Identity?.IsAuthenticated == true)
            {
                <button class="btn btn-logout" onclick="logout()" title="Çıkış Yap">
                    <span>🚪</span>
                    <span>Çıkış Yap</span>
                </button>
            }
            else
            {
                <a href="/Account/Login" class="btn btn-primary" style="width: 100%; justify-content: center;">
                    <span>🔐</span>
                    <span>Giriş Yap</span>
                </a>
            }

            <div class="footer-version">
                <div class="version-number">v1.0.0</div>
                <div>T-Soft API Connected</div>
            </div>
        </div>
    </aside>

    <div class="topbar">
        <button class="menu-toggle" onclick="toggleSidebar()" id="menuToggle">☰</button>
        <div class="topbar-brand">
            <div class="brand-icon">📦</div>
            <span>T-Soft ERP</span>
        </div>

        <!-- Mobile User Info & Logout -->
        <div class="topbar-user">
            <div class="topbar-user-name">@(User?.Identity?.Name ?? "Misafir")</div>
            @if (User?.Identity?.IsAuthenticated == true)
            {
                <button class="btn-icon" onclick="logout()" title="Çıkış Yap">
                    🚪
                </button>
            }
            else
            {
                <a href="/Account/Login" class="btn-icon" title="Giriş Yap">
                    🔐
                </a>
            }
        </div>
    </div>

    <main class="main-content">
        <button class="sidebar-toggle-desktop" onclick="toggleSidebar()" title="Menüyü Aç/Kapat">☰</button>
        @RenderBody()
    </main>

    <div class="loading-overlay" id="loading-overlay">
        <div class="loading-spinner"></div>
    </div>

    <script>
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            sidebar.classList.toggle('active');

            if (window.innerWidth > 768) {
                document.body.classList.toggle('sidebar-collapsed');
            }
        }

        function logout() {
            if (confirm('Çıkış yapmak istediğinizden emin misiniz?')) {
                document.getElementById('loading-overlay').classList.add('active');
                window.location.href = '/Account/Logout';
            }
        }

        document.addEventListener('click', function(event) {
            const sidebar = document.getElementById('sidebar');
            const menuToggle = document.getElementById('menuToggle');

            if (window.innerWidth <= 768 &&
                sidebar.classList.contains('active') &&
                !sidebar.contains(event.target) &&
                !menuToggle.contains(event.target)) {
                sidebar.classList.remove('active');
            }
        });

        document.querySelectorAll('.sidebar .nav-item').forEach(item => {
            item.addEventListener('click', function() {
                if (window.innerWidth <= 768) {
                    setTimeout(() => {
                        document.getElementById('sidebar').classList.remove('active');
                    }, 100);
                }
            });
        });
    </script>

    <style>
        .user-info-box {
            background: linear-gradient(135deg, rgba(124, 58, 237, 0.15) 0%, rgba(168, 85, 247, 0.15) 100%);
            border: 1px solid rgba(124, 58, 237, 0.3);
            border-radius: 12px;
            padding: 16px;
            margin-bottom: 12px;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .user-avatar {
            width: 48px;
            height: 48px;
            border-radius: 12px;
            background: linear-gradient(135deg, #7C3AED 0%, #EC4899 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            font-weight: 700;
            color: white;
            flex-shrink: 0;
            box-shadow: 0 4px 12px rgba(124, 58, 237, 0.3);
        }

        .user-details {
            flex: 1;
            min-width: 0;
        }

        .user-name {
            font-size: 14px;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 4px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .user-role {
            font-size: 11px;
        }

        .role-badge {
            display: inline-block;
            padding: 3px 8px;
            border-radius: 6px;
            font-weight: 600;
            font-size: 11px;
        }

        .role-admin {
            background: rgba(239, 68, 68, 0.15);
            color: #EF4444;
        }

        .role-manager {
            background: rgba(245, 158, 11, 0.15);
            color: #F59E0B;
        }

        .role-viewer {
            background: rgba(59, 130, 246, 0.15);
            color: #3B82F6;
        }

        .role-guest {
            background: rgba(160, 160, 160, 0.15);
            color: var(--text-secondary);
        }

        .btn-logout {
            width: 100%;
            padding: 12px 18px;
            border-radius: 10px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            border: 1px solid rgba(239, 68, 68, 0.3);
            background: rgba(239, 68, 68, 0.1);
            color: var(--danger);
            font-family: 'Inter', sans-serif;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            margin-bottom: 12px;
        }

            .btn-logout:hover {
                background: rgba(239, 68, 68, 0.2);
                border-color: var(--danger);
                transform: translateY(-2px);
                box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
            }

        .topbar-user {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .topbar-user-name {
            font-size: 13px;
            font-weight: 600;
            color: var(--text-primary);
            display: none;
        }

        @@media (min-width: 480px) {
            .topbar-user-name {
                display: block;
            }
        }

        .sidebar-close {
            display: none;
            width: 32px;
            height: 32px;
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: var(--text-secondary);
            font-size: 20px;
            cursor: pointer;
            align-items: center;
            justify-content: center;
            transition: all 0.2s;
        }

            .sidebar-close:hover {
                background: rgba(239, 68, 68, 0.2);
                color: var(--danger);
                border-color: var(--danger);
            }

        .sidebar-toggle-desktop {
            position: fixed;
            left: 280px;
            top: 16px;
            width: 40px;
            height: 40px;
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            z-index: 999;
            transition: all 0.3s;
            color: var(--text-secondary);
            font-size: 18px;
        }

            .sidebar-toggle-desktop:hover {
                background: var(--primary);
                color: white;
                border-color: var(--primary);
            }

        body.sidebar-collapsed .sidebar {
            transform: translateX(-280px);
        }

        body.sidebar-collapsed .main-content {
            margin-left: 0;
            width: 100%;
        }

        body.sidebar-collapsed .sidebar-toggle-desktop {
            left: 0;
        }

        @@media (max-width: 768px) {
            .sidebar-close {
                display: flex;
            }
        }
    </style>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>